# Session Log — 2026-04-11

## Merge upstream v2026.4.11-beta.1 + deploy

### Summary

Merged 1,244 upstream commits (v2026.4.9 → v2026.4.11-beta.1) from openclaw/openclaw into our fork. Security audit passed (all 4 areas SAFE). Clean merge with no conflicts. Deployed to both staging and VPS.

### Security Audit

| Area            | Rating | Notes                                                                                                                                                                                                                      |
| --------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Supply chain    | SAFE   | New deps: `@azure/identity`, `jsonwebtoken`, `jwks-rsa` (MS Teams auth), `nostr-tools`, `discord-api-types`. Axios pinned to 1.15.0 (security fix). All standard npm registry with sha512 integrity. No lifecycle scripts. |
| Docker/Build    | SAFE   | Dockerfile unchanged. `docker-compose.staging.yml` deleted by upstream (expected, survived locally). `OPENCLAW_BUNDLED_PLUGINS_DIR` override still required.                                                               |
| Code patterns   | SAFE   | No eval/backdoors/mining. All fetch/spawn calls legitimate. Security improvements: SSRF guards, token leak suppression, pairing hardening.                                                                                 |
| Config/Breaking | SAFE   | Large `src/plugins/types.ts` refactor (extracted to multiple files). MS Teams federated auth. Codex agent harness. No Telegram behavior changes.                                                                           |

### Key Changes

- **Import cycle cleanup** — Dozens of commits splitting heavy barrel imports into narrow runtime seams (reply-config, provider-model-types, session-hook-events, channel-registry). Should improve startup time and memory.
- **MS Teams** — Federated credential support (certificate + managed identity), SSO, reactions, message actions.
- **Codex extension** — New pluggable agent harness, app-server protocol, OAuth flow.
- **Security** — Axios 1.15.0 pin + dependency denylist, SSRF hostname validation, browser sandbox noVNC auth, expanded env security denylist.
- **Sandbox mode breaking change** — Now defaults to requiring Docker. Fixed by setting `agents.defaults.sandbox.mode=off` on both staging and VPS.

### Issues Encountered

1. **Local uncommitted changes blocking merge** — 5 extension files had leftover lint fixes from previous merge (redundant type constituents). The `qa-lab/bus-state.ts` change was broken (removed union members instead of fixing them). Discarded all local changes — upstream fixed these in the 1,244 commits.

2. **Sandbox mode requires Docker** — Staging bot reported cron job failure: `Sandbox mode requires Docker, but the "docker" command was not found in PATH`. Fixed by setting `agents.defaults.sandbox.mode=off` in both staging (`~/.openclaw-staging/openclaw.json`) and VPS (`/root/.openclaw/openclaw.json`) configs.

### Deployment

- **Staging:** Built `openclaw:local` image, gateway healthy.
- **VPS:** `git pull --rebase`, `docker compose build --no-cache`, `docker compose up -d`. Gateway healthy in 5.7s. All 5 Telegram bots running (mhivemain, mhiveresearch, mhivepoly, mhivebook, mhivedoost). 6 plugins loaded.

### Files Modified

- `TODO.md` — Added completed merge entry
- `~/.openclaw-staging/openclaw.json` — Set `agents.defaults.sandbox.mode=off`
- `/root/.openclaw/openclaw.json` (VPS) — Set `agents.defaults.sandbox.mode=off`
