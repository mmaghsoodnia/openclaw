# Session Log — 2026-03-07

## Summary

PolyHive agent pipeline recovery. SCORECARD showed Phase 1 FAILED (0/8 agents tool-validated,
~2 real trades in 3 weeks). Diagnosed 6 root causes and implemented 11-step fix plan.

## Root Causes Diagnosed

1. **Missing RUNBOOKs** — Scout, Analyst, Contrarian, Quant, Risk had no RUNBOOK.md at all
2. **RUNBOOK not auto-injected** — Only AGENTS.md/SOUL.md/TOOLS.md/IDENTITY.md/USER.md/HEARTBEAT.md/MEMORY.md are in the injection stack; agents must explicitly `read` RUNBOOK.md
3. **Wrong default model for all agents** — Everything used `xai/grok-4-1-fast`; Trader executing real-money trades needs a reasoning model; Scout a fast literal executor
4. **Broken venv activation** — `source venv/bin/activate` fails in cron/exec context; must use full python3 path
5. **Wrong data file paths** — Analyst/Contrarian/Quant/Risk TOOLS.md all pointed to stale `hive-trader/data/paper-trades.json` instead of canonical `the-hive/data/`
6. **Cron disabled** — From 2026-03-04 emergency kill switch; must be re-enabled after verification
7. **PM RUNBOOK corrupted** — Percy's RUNBOOK had FiatKillerHive crypto arb content instead of PolyHive procedures

## What Was Done

### Model assignments (openclaw.json on VPS)

- Added `"model": {"primary": "anthropic/claude-sonnet-4-5"}` to `hive-trader` entry in `agents.list`
- Added `"model": {"primary": "google/gemini-2.0-flash"}` to `hive-scout` entry in `agents.list`
- Removed an invalid `agents.agents` nested dict that was added earlier (caused config validation error)
- Gateway restarted to apply changes

### AGENTS.md (workspace-wide)

- Added step 5 to "Every Session" protocol: "If in MAIN SESSION and you have a RUNBOOK.md: Read it"

### RUNBOOKs written (VPS workspace)

- `hive-scout/RUNBOOK.md` — written from scratch: no-hallucination critical rule, full venv python path, output verification criteria (volume >1000, within 72h), forwarding format to Percy
- `hive-trader/RUNBOOK.md` — rewritten: full python path (never `source activate`), canonical data path (`the-hive/data/`), balance check, exit rules, reporting to Percy
- `hive-pm/RUNBOOK.md` — replaced FiatKillerHive content with PolyHive morning protocol, pipeline dispatch chain, escalation rules, anti-patterns
- `hive-analyst/RUNBOOK.md` — written from scratch: thesis format template, EV calculation, pass criteria, data sources
- `hive-contrarian/RUNBOOK.md` — written from scratch: challenge format template, fixture verification, volume check rule
- `hive-quant/RUNBOOK.md` — written from scratch: Kelly formula, response template, hard rules (EV<5%=PASS, EV>30%=flag)
- `hive-risk/RUNBOOK.md` — written from scratch: approval checklist (6 items), hard limits ($2/$10/$2), emergency halt procedure

### TOOLS.md updates (all 8 PolyHive agents)

All 8 TOOLS.md files now have a "READ YOUR RUNBOOK FIRST" block prepended with:

- RUNBOOK path
- Agent-specific critical rule (always-active, visible without reading RUNBOOK)
- Correct full python path reminder (where applicable)

Additional path fixes in Analyst/Contrarian/Quant/Risk TOOLS.md:

- `source venv/bin/activate` → full python3 path
- `hive-trader/data/paper-trades.json` → `the-hive/data/paper-trades.json`

### Notification

- Operator message sent via mhive bot to Mehdi's DM (message ID 1126)
- Asks Percy to read RUNBOOK and dispatch Scout for a test scan

## Files Modified (VPS — all under `/root/.openclaw/`)

| File                                   | Change                                           |
| -------------------------------------- | ------------------------------------------------ |
| `openclaw.json`                        | Per-agent model overrides in agents.list entries |
| `workspace/AGENTS.md`                  | Every Session step 5: read RUNBOOK.md            |
| `workspace/hive-scout/RUNBOOK.md`      | Written from scratch                             |
| `workspace/hive-trader/RUNBOOK.md`     | Rewritten (fixed venv + data paths)              |
| `workspace/hive-pm/RUNBOOK.md`         | Replaced FiatKillerHive content                  |
| `workspace/hive-analyst/RUNBOOK.md`    | Written from scratch                             |
| `workspace/hive-contrarian/RUNBOOK.md` | Written from scratch                             |
| `workspace/hive-quant/RUNBOOK.md`      | Written from scratch                             |
| `workspace/hive-risk/RUNBOOK.md`       | Written from scratch                             |
| `workspace/hive-*/TOOLS.md` (×8)       | RUNBOOK pointer prepended + path fixes           |

## Files Modified (local)

- `TODO.md` — Added completed entry for PolyHive pipeline fix
- `mhive-ops/sessions/2026-03-07.md` — This file

## Remaining

- **Cron re-enable (Step 11):** Cron stays disabled (`cron.enabled: false`) until Percy confirms
  Scout ran the script and the pipeline chain works end-to-end. Resume with:
  `{"cron":{"enabled":true},"agents":{"defaults":{"heartbeat":{"every":"30m"}}}}`
  Send as gateway `config.patch` via mhive.

- **Staging sync:** TOOLS.md and RUNBOOK changes are on VPS only. If staging needs them, run
  `bash mhive-ops/staging/sync-workspace.sh` then `bash mhive-ops/staging/rebuild.sh`.

- **SCORECARD verification:** After pipeline test, update SCORECARD.md if agents reach Phase 1 pass
  criteria (all 8 tool-validated).
