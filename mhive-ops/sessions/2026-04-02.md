# Session Log — 2026-04-02

## Summary

Two tasks completed this session:

### 1. Wire mhiveresearchbot token from 1Password

A new Telegram bot `@mhiveresearchbot` was created. Wired its token from 1Password through the standard env var injection pipeline and added the account + binding to `openclaw.json` on VPS.

**Changes made:**

- `mhive-ops/.env.vps.tpl` — added `TELEGRAM_MHIVERESEARCH_BOT_TOKEN={{ op://OpenClaw/Telegram Bot mhiveresearchbot/credential }}`
- Also fixed a stale item name in the same file: `Telegram bot mhivebrand` → `Telegram Bot mhivebrandbot`
- `mhive-ops/ARCHITECTURE.md` — added `mhiveresearch` to the accounts list, env var docs, and Telegram bindings table
- VPS `docker-compose.override.yml` — added `TELEGRAM_MHIVERESEARCH_BOT_TOKEN` to both `openclaw-gateway` and `openclaw-cli` environment blocks
- VPS `/root/.openclaw/openclaw.json` — added `mhiveresearch` account entry:
  ```json
  "mhiveresearch": {
    "botToken": "${TELEGRAM_MHIVERESEARCH_BOT_TOKEN}",
    "dmPolicy": "allowlist",
    "groupPolicy": "allowlist",
    "allowFrom": ["217834570"],
    "streaming": "off"
  }
  ```
  Added binding to `agentId: vega` (researcher agent).

Committed as `e12fa184ce` — pushed to fork, pulled on VPS, re-injected `.env`, gateway restarted. Bot confirmed live: `[mhiveresearch] starting provider (@mhiveresearchbot)`.

**Side fix:** During `op inject`, discovered the 1Password item for mhivebrand had been renamed. Fixed the item name reference in `.env.vps.tpl` from `Telegram bot mhivebrand` to `Telegram Bot mhivebrandbot`.

---

### 2. Lock down all Telegram bots to owner-only (217834570)

Audited all Telegram account security settings. Found:

- `mhivemain`, `mhivebook`, `mhivedoost` had `groupPolicy: open` — anyone in a group could trigger them
- `mhiveresearch` was fully open (`dmPolicy: open`, `allowFrom: ["*"]`)

**Fix:** Updated VPS `/root/.openclaw/openclaw.json` to set `dmPolicy: allowlist`, `groupPolicy: allowlist`, `allowFrom: ["217834570"]` on all affected accounts. The gateway hot-reloaded the config change automatically without a full restart. No config errors.

Final security state — all bots respond only to Telegram ID `217834570`:

| Account       | dmPolicy  | groupPolicy | allowFrom      |
| ------------- | --------- | ----------- | -------------- |
| mhivemain     | allowlist | allowlist   | [217834570]    |
| mhivepoly     | allowlist | allowlist   | [217834570]    |
| mhivebook     | allowlist | allowlist   | [217834570]    |
| mhivedoost    | allowlist | allowlist   | [217834570]    |
| mhiveresearch | allowlist | allowlist   | [217834570]    |
| default       | pairing   | allowlist   | (pairing flow) |

---

## Files Modified

**In git (committed + pushed):**

- `mhive-ops/.env.vps.tpl`
- `mhive-ops/ARCHITECTURE.md`

**On VPS only (not in git by design):**

- `/root/openclaw/docker-compose.override.yml`
- `/root/.openclaw/openclaw.json`

## Remaining / Notes

- `mhivedoost` was observed throwing `Expected ',' or ']' after array element in JSON` errors in the embedded agent run logs. This is a separate issue unrelated to this session's work — likely a JSON parsing bug in the agent's tool responses. Needs investigation in the mhivedoost agent context.
