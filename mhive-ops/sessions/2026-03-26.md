# Session Log — 2026-03-26

## Machine: Mac

## Summary

Merged upstream openclaw v2026.3.25-dev (~1,355 commits in two passes, 2026.3.14 → 2026.3.25-dev). Clean merges, no conflicts. Second pass picked up 85 additional security/test commits.

## What was done

### Upstream merge review + merge

- Fetched upstream/main (twice — 101 additional commits landed between first and second fetch)
- Reviewed all changes for VPS relevance: Telegram fixes, Discord fixes, gateway/core, security, performance, new features
- Merged cleanly with `git merge upstream/main` (commit `4a4e5da426`)

### Key upstream changes in this merge

**Telegram fixes (our primary channel):**

- Forum topic routing recovery (#53699)
- Photo preflight — falls back to document sends (#52545)
- 403 error handling — stops retrying doomed chats (#53635)
- DM auth enforced for callbacks (#55112)
- Native slash commands against runtime snapshot (#53179)
- Thread routing preserved through restarts (#53940)
- Verbose tool summaries in forum topics (#43236)
- Token fallback for binding-created accounts (#54362)

**Discord fixes:**

- Uncaught gateway errors no longer crash process
- Error listener leak fixed on teardown
- Force fresh gateway reconnects (#54697)

**Security (11+ fixes):**

- Skill installer validation with strict regex allowlists (#53471)
- Canvas routes require auth, agent reset requires admin scope
- Telegram DM auth for callbacks (#55112)
- Feishu webhook signature validation (#55083)
- Untrusted .env filtering before startup (#54631)
- Silent reconnect scope-upgrade blocked (#54694)
- Path traversal rejection in media parse (#54642)
- Telegram writeback admin scope gate (#54561)
- Sandbox alsoAllow/re-allows honored (#54492)
- Host-env blocklist for auth-profile env refs (#54627)
- Voice config writes require operator.admin scope (#54461)

**Performance:**

- SQLite memory hot-path optimization (#53939)
- LanceDB proxy bootstrap (#54119)

**New features:**

- Microsoft Foundry provider with Entra ID auth (#51973)
- Video generation core infrastructure (#53681)
- MiniMax image generation (#54487)
- OpenAI-compatible /v1/models and /v1/embeddings endpoints
- Slack file upload action (#54987)
- MSTeams search message + thread history

**Breaking:** ClawHub plugin installs preferred over npm (low risk for us)

### Documentation updates

- Updated `TODO.md` with completed merge entry
- Updated `MEMORY.md` current versions (was stale at 2026-03-13)
- Wrote this session log

### gog MCP investigation

- Investigated how gog is integrated: external CLI binary invoked via Lobster plugin (subprocess), NOT built into openclaw
- openclaw has native MCP stdio support (`src/agents/mcp-stdio.ts`)
- Identified migration path: replace Lobster+gog CLI with our gog-mcp Node.js server as an MCP server in openclaw.json
- Benefits: no shell subprocess layer, structured tool schemas, we control the code
- Migration not executed this session — planned for future

## Files modified

- `TODO.md` — added completed merge entry
- `MEMORY.md` (auto-memory) — updated Current Versions section
- `mhive-ops/sessions/2026-03-26.md` — this file

### Second merge pass (85 additional commits)

- Stashed doc changes, merged upstream/main again, restored stash
- Key additions: HTTP session ownership required (#55308), session history scope alignment (#55285), pairing required for backend scope upgrades (#55286), subagent session deletion caller scope (#55281), least-privilege plugin HTTP scopes (#55284), Telegram webhook auth throttling (#55142), WhatsApp quoted wrapper unwrap, session reset metadata preservation (multiple commits)

## What remains

- Push to fork (`git push origin main`)
- Deploy to VPS: pull, docker build, docker compose up -d
- Verify: `openclaw channels status --probe`, check gateway logs, test Telegram bots
- Future: migrate gog from Lobster CLI to MCP server
