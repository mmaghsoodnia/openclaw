# Session Log — 2026-03-10

## Summary

Implemented PolyHive agent health monitoring system (Layer 1 pipeline audit + Layer 2 business health).
All changes are VPS-only workspace files — no code changes, no gateway restart needed.

## What Was Done

### Context recovery

Previous session (2026-03-07) completed Phase 1 recovery: model assignments, 7 RUNBOOKs, 8 TOOLS.md updates.
This session continued with the approved health monitoring design.

### Research (before context loss)

- Verified VPS filesystem layout: session JSONL at `/root/.openclaw/agents/{id}/sessions/`, cron runs at `/root/.openclaw/cron/runs/{jobId}.jsonl`, jobs at `/root/.openclaw/cron/jobs.json`
- Found `hive-auditor` already exists with model=gemini-2.0-flash, `auditor-daily-audit` cron job already present (08:00 UTC, enabled:True but globally paused)
- Confirmed cron run log format: `{ts, jobId, status, error, summary, usage:{input_tokens, output_tokens, total_tokens}}`

### Changes executed (VPS — all under `/root/.openclaw/`)

| File                                | Change                                                                                                                                                                                                 |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `workspace/hive-auditor/RUNBOOK.md` | **Rewritten**: added PART A (Pipeline Health Audit with 6 KPI steps + AUDIT.md format) before existing PART B (Financial Reconciliation). Fixed paper trades path (was wrong `hive-trader/data/`).     |
| `workspace/hive-auditor/TOOLS.md`   | Added "Session Files & Data Paths" section with JSONL paths, cron run log format, trade ledger paths, and Python snippet for filtering last-24h sessions.                                              |
| `cron/runs/jobs.json`               | Updated `auditor-daily-audit` message to: read RUNBOOK first, run pipeline KPI audit (sessions count, cron log analysis, KPI computation, AUDIT.md write, Percy alert), then financial reconciliation. |
| `workspace/hive-pm/RUNBOOK.md`      | Added step 0 to morning protocol: read `the-hive/data/AUDIT.md`, handle RED/YELLOW/GREEN status before dispatching Scout.                                                                              |

### Files backed up

- `/root/.openclaw/cron/runs/jobs.json.bak2` (auto-created before update)

## KPI Thresholds (from design)

| KPI                      | Threshold            | Source                               |
| ------------------------ | -------------------- | ------------------------------------ |
| Pipeline completion rate | >70%                 | session JSONL                        |
| Tool execution rate      | >80%                 | session JSONL `type:tool_call` lines |
| Timeout rate             | <10%                 | cron run `error:timeout`             |
| Token cost per pipeline  | <10k tokens          | cron run `usage.total_tokens`        |
| Session creation rate    | All 8 agents > 0     | agents dir mtime                     |
| Win rate                 | >50% over 20+ trades | real-trades.json                     |

## Remaining

- **Cron re-enable (Step 11 from Phase 1):** Cron stays globally disabled until Percy confirms
  Scout ran the script and the pipeline chain works end-to-end. Resume with:
  `{"cron":{"enabled":true},"agents":{"defaults":{"heartbeat":{"every":"30m"}}}}`
  Send as gateway `config.patch` via mhive.

- **AUDIT.md first run:** Will be empty until `auditor-daily-audit` fires at 08:00 UTC.
  Once cron is re-enabled, auditor will produce its first AUDIT.md the next morning.

- **Staging sync:** If staging workspace needs these changes:
  `bash mhive-ops/staging/sync-workspace.sh && bash mhive-ops/staging/rebuild.sh`

- **Lower priority (TODO.md):** Mac Studio staging, voice for agents.

---

## Session 2 — VPS Security Hardening

### SSH key setup

- Added this Mac's public key (`mm-macir`, ed25519) to VPS `~/.ssh/authorized_keys`
- Fixed concatenated key (was appended without newline to previous line)
- Note: public IP SSH (76.13.79.239) doesn't work from this Mac because Tailscale intercepts and routes through tunnel. Use Tailscale IP (`100.71.224.113`) instead.

### VPS firewall hardening

| Change                  | Details                                                                                                                                                                                                                                        |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **DOCKER-USER chain**   | Locked Docker-published ports (18789-18790) to Tailscale only. Rules: allow `tailscale0` interface, allow `lo`, allow `172.16.0.0/12` (Docker internal), DROP all else. Persistence script at `/etc/network/if-pre-up.d/docker-user-firewall`. |
| **SSH hardened**        | `/etc/ssh/sshd_config.d/01-hardening.conf`: `PasswordAuthentication no`, `PermitRootLogin prohibit-password`, `MaxAuthTries 3`, `LoginGraceTime 20`, `KbdInteractiveAuthentication no`. Backup at `sshd_config.bak.2026-03-10`.                |
| **cloud-init override** | Renamed `hardening.conf` → `01-hardening.conf` so it loads before `50-cloud-init.conf` (which had `PasswordAuthentication yes`). OpenSSH first-match-wins.                                                                                     |
| **fail2ban installed**  | `jail.local`: sshd jail enabled, 3 retries → 24h ban, Tailscale IPs whitelisted (`100.64.0.0/10`).                                                                                                                                             |

### Hostinger MCP server setup

- Installed `hostinger-api-mcp` globally via npm (Node.js package, not a binary)
- Created launcher `mhive-ops/hostinger-mcp-run.sh` — injects API_TOKEN from 1Password at runtime (`op://OpenClaw/Hostinger API Key/credential`)
- Registered in `~/.claude.json` as `mcpServers.hostinger` (same pattern as gog)
- **Requires Claude Code restart** to load the new MCP server

### Hostinger cloud firewall configured (via API)

- Firewall group: `mhive` (ID: 234443) on VPS 1389008
- Rule 1: Accept TCP 22 (SSH) from any — rule ID 809012
- Rule 2: Accept UDP 41641 (Tailscale WireGuard) from any — rule ID 809014
- Implicit default: DROP all other inbound
- Activate action: success (action IDs 83132713, 83132894)
- All outbound connections (Google APIs, Telegram, 1Password, Polymarket) unaffected — Hostinger firewall is inbound-only
- Note: `is_synced` stays false in API response even after successful activate — appears to be an API quirk
- Cannot verify blocking from this Mac because Tailscale intercepts all traffic to VPS (routes through tunnel regardless of IP used)

### Hostinger MCP server installed

- Installed `hostinger-api-mcp` globally via npm
- Launcher: `mhive-ops/hostinger-mcp-run.sh` — injects API_TOKEN from 1Password at runtime
- Registered in `~/.claude.json` as `mcpServers.hostinger`
- MCP tools did not load in this session (likely 1Password Touch ID timeout at startup) — used direct API calls instead
- Available for future sessions after Claude Code restart

### WhatsApp removed from TODO

- Removed re-enable WhatsApp item from `TODO.md` per user request
