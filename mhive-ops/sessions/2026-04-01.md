# Session — 2026-04-01

## Machine: Mac

## Summary

Merged upstream v2026.4.1 (964 commits) into our fork using the `updateOpenClaw` skill. Security audit clean. Deployed to both staging and VPS successfully.

## What was done

### Upstream merge v2026.4.1

- Fetched upstream: 964 new commits since v2026.3.28
- New release tag: `v2026.3.31` (version in package.json: `2026.4.1`)
- Security audit via agent: all commits SAFE, no malicious code found
- One merge conflict: `.agents/skills/openclaw-test-heap-leaks/` (untracked local files vs upstream additions) — resolved by accepting upstream version
- `pnpm install` + `pnpm tsgo` clean

### Key upstream changes

- **SearXNG web search provider** — new bundled plugin (#57317)
- **Exec approval system** — Discord, Telegram, webchat approval flows
- **Major security hardening:**
  - exec/spawn/shell/fs_write/fs_delete denied by default via HTTP tool invoke
  - Trusted-proxy origin checks enforced
  - Sessions revoked on token rotation
  - Sandbox env sanitized
  - Auth env vars blocked from workspace dotenv
  - Multiple credential redaction improvements
- **Configurable chat history max chars** (#58900)
- **Memory session indexing fix** (#39732)
- **Bundled channel plugin compat fix** (#58873)
- **Node pairing reconciliation refactor**
- **Matrix E2EE** — `@matrix-org/matrix-sdk-crypto-wasm` added
- **Internal hooks** now enabled by default (4 handlers loaded)
- **`@aws-sdk/client-bedrock`** removed
- **`pnpm.minimumReleaseAge`** removed

### Deployment

- Staging: rebuilt, gateway healthy
- VPS: pulled, rebuilt, `docker compose up -d`, gateway healthy
- All 4 Telegram bots running: mhivemain, mhivebook, mhivepoly, mhivedoost
- `OPENCLAW_BUNDLED_PLUGINS_DIR=/app/dist/extensions` workaround still in place and still required

### Notes

- VPS warning about `TELEGRAM_MHIVERESEARCH_BOT_TOKEN` not set — env var reference was added to `docker-compose.override.yml` by another agent but the 1Password item/env template entry doesn't exist yet. Non-critical (defaults to blank string).
- Created `updateOpenClaw` skill at `.agents/skills/updateOpenClaw/SKILL.md` earlier in this session for repeatable upstream merge workflow.

## Files modified

- `TODO.md` — added v2026.4.1 merge entry
- `mhive-ops/sessions/2026-04-01.md` — this file

## Commits

- `73ab3910c6` — "Merge remote-tracking branch 'upstream/main'"
- (documentation commit TBD)
